export type Product = {
  category: string;
  price: number;
  stocked: boolean;
  name: string;
};

export type Fee = {
  category: string;
  price: number;
  nameOfFee: string;
  description: string;
}